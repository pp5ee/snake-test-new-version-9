# Requirement

Now I want to create a Snake game. Keep the code as simple as possible. It should support score recording stored locally on the page, so that when I open it again next time, I can still see my score history. That’s all — this is just demo code. Force the use of RLCR mode.

Plain HTML/CSS/JS，retro pixel look,Default snake speed, arrow key controls，ust the top 5-10 high scores

---

## Standard Deliverables (mandatory for every project)

- **README.md** — must be included at the project root with: project title & description, prerequisites, installation steps, usage examples with code snippets, configuration options, and project structure overview.
- **Git commits** — use conventional commit prefix `feat:` for all commits.
